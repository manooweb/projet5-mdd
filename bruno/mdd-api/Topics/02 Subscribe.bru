meta {
  name: Subscribe
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/topics/{{topicId}}/subscription
  body: none
  auth: none
}

vars:pre-request {
  topicId: 9
}

tests {
  test("status is 204", function () {
    expect(res.status).to.equal(204);
  });
}
