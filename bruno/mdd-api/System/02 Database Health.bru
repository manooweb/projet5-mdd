meta {
  name: Database Health
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/actuator/health/db
  body: none
  auth: none
}

tests {
  test("status is 200", function () {
    expect(res.status).to.equal(200);
  });

  test("database is up", function () {
    expect(res.body.status).to.equal("UP");
  });
}
